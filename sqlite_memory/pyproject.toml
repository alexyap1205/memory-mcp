[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sqlite-memory"
version = "0.1.0"
description = "SQLite-based memory storage backend"
dependencies = [
    "fastmcp>=0.1.0",
]

[tool.hatch.build.targets.wheel]
packages = ["sqlite_memory"]
